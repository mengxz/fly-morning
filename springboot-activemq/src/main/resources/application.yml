server:
  port: 7777

spring:
  activemq:
    broker-url: tcp://127.0.0.1:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false  #false=不是订阅模式，走Queue；true=订阅模式，走Topic

#队列名称
myqueue: boot-mq-queue
